---
- name: 'Reload systemd'
  ansible.builtin.systemd:
    daemon_reload: true

- name: 'Restart openvpn-exporter'
  ansible.builtin.systemd:
    name: 'openvpn-exporter.service'
    state: restarted
